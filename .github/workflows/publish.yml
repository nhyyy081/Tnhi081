name: 🚀 Deploy to Somee via FTP

on:
  # Kích hoạt workflow khi có push lên nhánh 'main'
  push:
    branches:
      - main
  
  # Cho phép chạy workflow thủ công từ giao diện GitHub
  workflow_dispatch:

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    
    environment: 
      name: production 

    steps:
      - name: 🚚 Checkout code
        # Lấy mã nguồn từ repo
        uses: actions/checkout@v4

      # --- BƯỚC BUILD (NẾU CẦN THIẾT) ---
      # Bỏ qua các dòng sau nếu dự án của bạn chỉ là file HTML/PHP tĩnh
      # - name: 📦 Build Project (Ví dụ cho dự án Node/React/Vue)
      #   run: |
      #     npm install
      #     npm run build
      
      - name: 🚀 FTP Deploy to Somee.com
        # CẤU HÌNH ĐÃ SỬA LỖI CÚ PHÁP
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          # SỬ DỤNG TÊN SECRETS BẠN ĐÃ ĐẶT
          server: ${{ secrets.FTP_HOST }} 
          username: ${{ secrets.FTP_USER }} 
          password: ${{ secrets.FTP_PW }} 
          
          # THƯ MỤC NGUỒN: Thay đổi nếu bạn có thư mục build (e.g., ./dist)
          local-dir: ./ 
          
          # THƯ MỤC ĐÍCH: Kiểm tra lại đường dẫn đích trên Somee.com
          remote-dir: / 
          
          log-level: verbose
